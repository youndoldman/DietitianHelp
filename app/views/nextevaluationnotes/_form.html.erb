<%= form_for(nextevaluationnote) do |f| %>
  <% if nextevaluationnote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nextevaluationnote.errors.count, "error") %> prohibited this nextevaluationnote from being saved:</h2>

      <ul>
      <% nextevaluationnote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nnote %>
    <%= f.text_area :nnote %>
  </div>

  <div class="field">
    <%= f.label :client_id %>
    <%= f.text_field :client_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
