<%= form_for(progressnote) do |f| %>
  <% if progressnote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(progressnote.errors.count, "error") %> prohibited this progressnote from being saved:</h2>

      <ul>
      <% progressnote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pnote %>
    <%= f.text_field :pnote %>
  </div>

  <div class="field">
    <%= f.label :client_id %>
    <%= f.text_field :client_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
