<%= form_for(ajaxget) do |f| %>
  <% if ajaxget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ajaxget.errors.count, "error") %> prohibited this ajaxget from being saved:</h2>

      <ul>
      <% ajaxget.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
